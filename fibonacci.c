#include <stdio.h>

int fib(int a, int b){
	return a+b;
	}

int main(int argc, char ** argv){
	printf("/nPrints out fibonacci sequence/n");
	int i = 0;
	int previous = 0;
	while(i<10)
		if( prev!=0)
			return fib(1,1);
		else
			fib = fib(i, fib(prev)
		fib(i, fib(i-1));
	}
